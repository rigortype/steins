{
    "name": "acme/app",
    "description": "The plugin-channel tracer fixture (issue #68): a project that requires one steins-plugin package.",
    "require": {
        "acme/cache": "^1.0",
        "acme/steins-plugin": "^1.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}
