{
    "name": "acme/app",
    "description": "The ADR-0079 dam vendor-presumption fixture (issue #181): a file that fails to parse under the declared `3rdparty` vendor-dir must not dam the existence family, exactly as it would not under a literal `vendor/`.",
    "config": {
        "vendor-dir": "3rdparty"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}
