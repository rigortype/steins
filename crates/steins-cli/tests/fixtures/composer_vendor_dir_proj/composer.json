{
    "name": "acme/app",
    "description": "The composer-aware vendor-dir resolution fixture (issue #181): the declared vendor-dir is `3rdparty`, not the literal `vendor`.",
    "config": {
        "vendor-dir": "3rdparty"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}
