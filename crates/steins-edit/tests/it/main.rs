//! The crate's integration tests, compiled as one binary (xtask's `test_layout` guard).

mod effects_envelope;
mod envelope;
mod honesty;
mod honesty_methods;
mod loops;
mod partitions_identity;
mod promote;
mod promote_methods;
