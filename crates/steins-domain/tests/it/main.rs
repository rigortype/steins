//! The crate's integration tests, compiled as one binary (xtask's `test_layout` guard).

#![allow(clippy::too_many_lines, reason = "test code is outside the #778 ratchet")]

mod lattice;
mod lean_vectors;
mod php_oracle;
