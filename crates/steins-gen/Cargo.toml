[package]
name = "steins-gen"
description = "Frozen-generation substrate (ADR-0092 §2): identity, artifact container, candidate-then-publish."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
blake3.workspace = true

# Only for the two clone syscalls (`share.rs`); every other path here is std.
[target.'cfg(unix)'.dependencies]
libc.workspace = true
