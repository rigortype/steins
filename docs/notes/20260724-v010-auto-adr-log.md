# v0.1.0 run — auto-ADR decision log

Convention for the continuous-implementation run toward v0.1.0
(PHPStan-grade checker; ROADMAP M1+M2, release gate M3): significant
design items encountered mid-run are commissioned to Fable subagents
as they arise; the resulting ADRs are committed immediately with
binding effect so implementation never blocks, and **every such ADR is
logged here** (number, one-line thrust, key decisions, open questions)
for the owner's batch review. Ratification states: PENDING → RATIFIED
/ AMENDED / VETOED (a veto reverts the ADR and its dependent slices).
One file for the whole run; newest entries appended at the bottom.

## Carried in from the planning session (pre-run)

- **ADR-0047 project partitioning** — regions/nameability/vouching.
  Owner answered the four design questions in-session (runtime
  disjointness confirmed; assignments are config-only; vendor
  presumption default-on; observers stay in the candidate domain via
  the no-observed-callers generalization). **RATIFIED** (in-session).
- **ADR-0048 position-query architecture** — replay over retention;
  three binding constraints on all inference work. **PENDING** (no
  objections raised; constraints already enforced in delegations).
- **ADR-0049 finding-breadth family** — two existence oracles, dam-free
  `call.undefined-method`, proven-E_WARNING-is-proof-layer. The
  warning-handler pseudo-constant amendment (`[runtime]
  warning-handler`, default "abort") was owner-directed. **RATIFIED**
  (E_WARNING posture explicitly; remainder unobjected).
- **ADR-0050 diagnostic layers + profiles** — proof/contract/mechanics
  layers, profiles as data, G1 deliberately undecided. **PENDING**
  (open: check-side vs doctor-only unchecked-envelope notice; warn-only
  exit code 0 vs distinct; strict-vs-boundary profile name).
- **ADR-0051 template scope transfer** — declared-bridge model,
  registration vs recognition strata. **PENDING** (open: vars_arg
  omission semantics; unresolvable-registration error-vs-warning;
  render-helper recon before the recognition slice).
- **ADR-0052 narrowing + subtraction** — three fact carriers,
  min-stratum premises, Asserted-by-default assert(). **PENDING**
  (open: zend-assertions autodetect vs manual; asserted-premise
  finding family; static-props scope; N4-vs-S6 landing order).

## Run entries

<!-- append: ### ADR-00NN <title> — <date>
     Thrust: … / Key decisions: … / Open questions: … / Status: PENDING -->
